#include<iostream>
using namespace std;

int main(){
    // 115. Distinct Subsequences
        // T.C>>O(2^n*2^m) S.C>>O(n+m)
    // memorization->T.C-> O(n*m) S.C->O(n+m)+O(n*m)
    // Tabulation->T.C-> O(n*m) S.C->O(n*m)
    // Tabulation+SO ->T.C-> O(n*m) S.C->O(m)

    // 72. Edit Distance
    // (n+m)  is the maximum operation

    // 44. Wildcard Matching
    return 0;
}