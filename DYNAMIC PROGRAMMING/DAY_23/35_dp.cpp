#include<iostream>
using namespace std;

int main(){
    // 62. Unique Paths
    // 63. Unique Paths II
    // 64. Minimum Path Sum
    // 120. Triangle   T.C=O(2^(n*n)) S.C=O(n)
    // Memorisation T.C=O(n*n) S.C=O(n)+O(n*n)
    // Tabulation T.C=O(n*n) S.C=O(n*n)
    // Tabulation + Space Optimization T.C=O(n*n) S.C=O(n)
    return 0;
}