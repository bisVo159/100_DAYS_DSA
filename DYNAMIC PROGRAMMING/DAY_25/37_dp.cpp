#include<iostream>
using namespace std;

int main(){
    // Subset Sum Equal To K  -> https://www.codingninjas.com/studio/problems/subset-sum-equal-to-k_1550954
     // T.C-> O(2^n) S.C->O(n)
    // memorization->T.C-> O(n*k) S.C->O(n)+O(n*k)
    // Tabulation->T.C-> O(n*k) S.C->O(n*k)
    // Tabulation+SO ->T.C-> O(n*k) S.C->O(k)

    // 416. Partition Equal Subset Sum  -> same T>C and S.C

    //  Array partition with minimum difference -> https://www.codingninjas.com/studio/problems/partition-a-set-into-two-subsets-such-that-the-difference-of-subset-sums-is-minimum_842494


    return 0;
}