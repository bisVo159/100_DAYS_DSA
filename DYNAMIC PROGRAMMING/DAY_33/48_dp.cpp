#include<bits/stdc++.h>
using namespace std;

int main(){
    //  Boolean Evaluation -> https://www.codingninjas.com/studio/problems/problem-name-boolean-evaluation_1214650
          // memorization->T.C-> O(n*n*2)*n S.C->O(n)+O(n*n)
    // Tabulation->T.C-> O(n*n) S.C->O(n*n)

    // 132. Palindrome Partitioning II
    // memorization->T.C-> O(n*n)*n S.C->O(n)+O(n)
    // Tabulation->T.C-> O(n*n)*n S.C->O(n)

    // 1043. Partition Array for Maximum Sum
        // memorization->T.C-> O(n*k)*n S.C->O(n)+O(n)
    // Tabulation->T.C-> O(n*k)*n S.C->O(n)
    return 0;
}