#include<iostream>
using namespace std;

int main(){
    // 322. Coin Change       Minimum Elements->https://www.codingninjas.com/studio/problems/minimum-elements_3843091
    // T.C>>O(2^n) S.C>>O(n)
    // memorization->T.C-> O(n*amount) S.C->O(n)+O(n*amount)
    // Tabulation->T.C-> O(n*amount) S.C->O(n*amount)
    // Tabulation+SO ->T.C-> O(n*amount) S.C->O(amount)

    // 494. Target Sum
    // T.C>>O(2^n) S.C>>O(n) worst case O(amount)
    // memorization->T.C-> O(n*amount) S.C->O(amount)+O(n*amount)
    // Tabulation->T.C-> O(n*amount) S.C->O(n*amount)
    // Tabulation+SO ->T.C-> O(n*amount) S.C->O(amount)

    return 0;
}