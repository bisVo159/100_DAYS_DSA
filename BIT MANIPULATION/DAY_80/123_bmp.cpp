#include<iostream>
using namespace std;

int main(){
    // 1979. Find Greatest Common Divisor of Array
    // All divisors of a Number -> gfg

    // Prime Factors -> gfg
    // O(sqrt(n)*logn)
    // 50. Pow(x, n)
    // O(logn)
    return 0;
}